# PostgreSQL persistence and email OTP

Use Neon PostgreSQL with Drizzle and Better Auth email OTP, deployed with TanStack Start on Vercel Node. The user approved the stack despite greater familiarity with Prisma; keep persistence isolated and migrations explicit. Resend will send from login@auth.davidmagbee.com after DNS verification; account and guest identities remain separate.
